# FarmTwin engine — core Python dependencies
# Primary source: pyproject.toml [project.dependencies].
# Keep this legacy mirror in sync for direct pip users.
#
# The core engine stays lean (R-NFR-9): only what the implemented engine
# imports at runtime. Optional features live in pyproject extras:
#   pip install -e ".[science]"  # scipy — richards adaptive ODE integrator
#   pip install -e ".[viz]"      # matplotlib — postprocess plots
#   pip install -e ".[pro]"      # wntr, tsnet, pymoo, pyfao56, ioos-qc backends

# ── Numerics (solver core) ──────────────────────────────────────────
numpy>=1.26,<2.0

# ── Schema validation (FTS survey schema) ───────────────────────────
jsonschema>=4.22
